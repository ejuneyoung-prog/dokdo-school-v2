# 이 저장소에서 일하는 방식

## 운영자(교수님)가 직접 다루는 코드는 통째로 된 파일로만 준다

구글 Apps Script처럼 교수님이 직접 붙여넣고 배포하시는 코드는 **언제나 전체
파일 하나**로 드린다. "이 조각을 기존 함수 맨 앞에 넣으세요" 같은 안내는 금지다.
교수님이 기존 코드를 한 줄씩 대조해야 하는 부담을 지우기 때문이다.

- 파일 전체를 만들어 `SendUserFile`로 보낸다. 교수님은 **전체 선택 → 붙여넣기 →
  새 버전 배포**만 하시면 되어야 한다.
- 기존 동작(활동기록·주간순위·명예의전당 등)을 그 파일 안에 모두 포함한다.
  빠뜨리면 교수님 사이트의 기능이 죽는다.
- 되돌리는 방법을 함께 적는다. Apps Script는 버전 기록이 남으므로 이전 버전으로
  되돌릴 수 있다는 점을 안내한다.
- 추측으로 채우지 않는다. 기존 코드를 모르면 만들어내지 말고 여쭙는다.

## 배포 관련

- `main`에 합치기 전에 되돌리는 방법을 먼저 정리해 드린다.
- 브라우저 검사(chromium)는 로컬에서 돌릴 수 있고, webkit은 이 환경에 없어
  CI 결과로만 확인된다. 확인되지 않은 것을 통과했다고 말하지 않는다.

## 문항 내용

- 원본은 `dokdo-next/tools/content/authored-questions.json` 하나뿐이다.
  생성물(`data/course-v2.json`, `assets/course-data.js`)은 직접 고치지 않는다.
- 고친 뒤 반드시 `python3 tools/content/build_course.py`로 다시 만든다.
- 출처에 없는 사실을 지어내지 않는다. 문제를 위해 상황을 지어내지도 않는다.

## 사용자 기록

- 승급 공식과 등급 사다리는 줄이지 않는다. 기존 학습자가 강등된다.
- 남의 기록을 복원하는 일은 되돌리기 어렵다. 실행 전에 확인을 받는다.
